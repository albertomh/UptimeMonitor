{
    "name": "uptime-monitor",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev --local --host index.ts --test-scheduled",
        "deploy": "wrangler deploy index.ts",
        "build": "esbuild index.ts --bundle --outfile=dist/index.js --platform=browser --format=esm --minify --loader:.txt=text",
        "d1:local:init": "wrangler d1 execute DB --local --file=schema.sql",
        "d1:local:seed": "wrangler d1 execute DB --local --file=seed.sql",
        "d1:local:reset": "rm -rf .wrangler/state && wrangler d1 execute DB --local --file=schema.sql && wrangler d1 execute DB --local --file=seed.sql",
        "types": "wrangler types"
    },
    "devDependencies": {
        "esbuild": "^0.28.2",
        "typescript": "^7.0.2",
        "wrangler": "^4.127.0"
    },
    "packageManager": "pnpm@11.24.0"
}
